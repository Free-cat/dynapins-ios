# Image source:
# - To pull from Docker Hub: leave DYNAPINS_IMAGE=freecats/dynapins-server:latest and run: make e2e-pull
# - To build locally from ../dynapins-server: set DYNAPINS_IMAGE=dynapins-server:local and run: make e2e-build
DYNAPINS_IMAGE=freecats/dynapins-server:latest

# Server config
SERVER_PORT=8080
ALLOWED_DOMAINS=example.com,*.example.com,api.example.io
SIGNATURE_LIFETIME=1h
LOG_LEVEL=debug

# Integration test config
TEST_DOMAIN=api.example.com
# TEST_PUBLIC_KEY will be auto-generated from private key

# Provide ECDSA P-256 (ES256) private key PEM content here (multiline is supported)
# PRIVATE_KEY_PEM=-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----

# Alternatively, leave empty and Makefile will generate one at /tmp/dynapins-e2e/private_key.pem
